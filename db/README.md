# Database Layer

This folder documents the database layer used by the backend.

Current plan:
- Prisma schema in `prisma/schema.prisma`
- Prisma client helper in `lib/server/db.js`
- seeds in `prisma/seed.js`

Database-backed features:
- users and auth
- profile data
- job application tracking
- resume-related metadata
